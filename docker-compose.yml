services:
  php82:
    build: .
    container_name: php82-simplesaml
    network_mode: host
    volumes:
      - .:/app
    working_dir: /app
    tty: true
    environment:
      SIMPLESAMLSERVER_CONFIG_DIR: /app/config
